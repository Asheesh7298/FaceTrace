# Environment
.env
*.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Node
node_modules/
blockchain/artifacts/
blockchain/cache/

# Pipeline outputs
output/
.cache/
cache/
artifacts/

# Deployment files (contain contract addresses — commit if you want)
# deployment_localhost.json  ← local only, don't commit
deployment_localhost.json

# Deployment on Sepolia IS useful to commit (public address)
# deployment_sepolia.json ← keep this one

# Model weights (auto-downloaded)
gfpgan/weights/
*.pth
*.onnx

# Curated DB: keep the built .npz + meta (reproducible), skip temp + stale local json
face_db/_tmp_ref.jpg
face_db/curated_index.json

# Logs
*.log

# OS
.DS_Store
Thumbs.db
