# Face Detection & Recognition
deepface==0.0.93
insightface==0.7.3
onnxruntime-gpu==1.18.0
facenet-pytorch==2.5.3
opencv-python==4.9.0.80
mtcnn==0.1.1

# Image Quality & Processing
Pillow==10.3.0
pillow-avif-plugin==1.4.6     # read AVIF inputs (common from Google Images)
pillow-heif==0.16.0          # read HEIC/HEIF inputs (iPhone photos)
scikit-image==0.22.0
scipy==1.13.1
imagehash==4.3.1
brisque==0.0.16
exifread==3.0.0

# Search & identity
requests==2.31.0
spacy==3.8.7                 # PERSON NER for identity extraction from Lens ai_overview
# after install run: python -m spacy download en_core_web_sm
playwright==1.44.0           # optional — Yandex/Bing boosters (ENABLE_YANDEX/BING=1)
# after install run: python -m playwright install chromium
boto3==1.34.100              # optional — only for the AWS Rekognition engine

# Blockchain (Python side)
web3==6.15.1
eth-account==0.11.0

# Web UI
fastapi==0.111.0
uvicorn==0.30.1
sse-starlette==2.1.0
python-multipart==0.0.9

# Utilities
numpy==1.26.4
python-dotenv==1.0.1
rich==13.7.1

# Modal (optional — cloud GPU DB search, not used in the default Lens pipeline)
modal>=0.64.0